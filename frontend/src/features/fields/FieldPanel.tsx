import type{Field}from"../../lib/types";
export function FieldPanel({service,fields}:{service?:string;fields:Field[]}){return <aside className="fields"><div className="panel-head"><span>Complaint details</span><strong>{service||"Not identified"}</strong></div>{fields.length===0?<p className="empty">Details will appear here as you talk.</p>:fields.map(f=><div className="field" key={f.id}><div><strong>{f.id.replaceAll("_"," ")}</strong><small>{f.required?"Required":"Optional"}{f.source?` · ${f.source}`:""}</small></div><span className={f.status}>{f.value==null||f.value===""?"Needed":String(f.value)}</span></div>)}</aside>}
