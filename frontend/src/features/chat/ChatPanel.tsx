import {FormEvent,useState} from "react"; import type{Message}from"../../lib/types";
export function ChatPanel({messages,onSend,busy}:{messages:Message[];onSend:(v:string)=>void;busy:boolean}){const[v,setV]=useState("");function submit(e:FormEvent){e.preventDefault();if(v.trim()){onSend(v.trim());setV("")}}return <section className="chat"><div className="messages" aria-live="polite">{messages.map((m,i)=><p key={i} className={`bubble ${m.role}`}><strong>{m.role==="citizen"?"You":"CivicAgent"}</strong>{m.text}</p>)}</div><form onSubmit={submit} className="composer"><label htmlFor="message">Describe the issue</label><div><input id="message" value={v} onChange={e=>setV(e.target.value)} disabled={busy} maxLength={4000}/><button disabled={busy||!v.trim()}>Send</button></div></form></section>}
