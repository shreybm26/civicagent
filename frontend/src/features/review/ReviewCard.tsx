import type{Field}from"../../lib/types";
export function ReviewCard({fields,department,onEdit,onSubmit,busy}:{fields:Field[];department?:string;onEdit:(id:string,v:string)=>void;onSubmit:()=>void;busy:boolean}){return <section className="review"><h2>Review your complaint</h2><p>Department: <strong>{department||"Defined by service schema"}</strong></p>{fields.map(f=><div className="review-row" key={f.id}><label>{f.id.replaceAll("_"," ")}<input value={String(f.value??"")} onChange={e=>onEdit(f.id,e.target.value)}/></label></div>)}<button className="primary" onClick={onSubmit} disabled={busy}>Submit complaint</button><small>Nothing is submitted until you press this button.</small></section>}
